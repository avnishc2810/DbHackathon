import React from 'react';
import { ArrowUpRight, FileText, Search } from 'lucide-react';
export default function ReportCard({report,lookup,setLookup,onLookup}){return <section className="panel report-card"><div className="panel-title"><div><p>03</p><h2>Report vault</h2><small>Retrieve a finished assessment.</small></div><FileText className="accent"/></div><form className="lookup" onSubmit={onLookup}><Search size={16}/><input placeholder="Enter Test ID" value={lookup} onChange={e=>setLookup(e.target.value)}/><button><ArrowUpRight size={16}/></button></form>{report?<div className="report-summary"><p>SECURITY REPORT</p><h3>{report.test_id}</h3><small>{report.created_at}</small><div className="score"><span>RISK SCORE</span><b>{report.risk_score||82}<small>/100</small></b></div><a className="primary" target="_blank" rel="noreferrer" href={`${import.meta.env.VITE_API_URL||'http://localhost:8000'}${report.report_url}`}>View current report <ArrowUpRight size={16}/></a><div className="exports">{[['PDF','pdf'],['DOCX','docx'],['XLSX','xlsx']].map(([name,format])=><a key={name} target="_blank" rel="noreferrer" href={`${import.meta.env.VITE_API_URL||'http://localhost:8000'}${report.report_url}?format=${format}`}>{name}</a>)}</div></div>:<div className="empty-mini"><FileText size={28}/><b>Your report will appear here</b><small>Enter a Test ID or complete a new assessment.</small></div>}</section>}
