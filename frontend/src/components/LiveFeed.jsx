import React from 'react';
import { Check, LoaderCircle } from 'lucide-react';
export default function LiveFeed({scan,events}){if(!scan)return null;const active=scan.status==='running';return <section className="panel feed"><div className="panel-title"><div><p>02</p><h2>{active?'Live intelligence feed':'Assessment complete'}</h2><small>Test ID <b>{scan.test_id}</b></small></div><strong className={active?'running':''}><i/>{active?'STREAMING':'REPORT READY'}</strong></div><div className="feed-list">{events.length?events.map((event,index)=><div className="feed-item" key={index}><i>{index===events.length-1&&active?<LoaderCircle className="spin" size={14}/>:<Check size={14}/>}</i><span><b>{event.message}</b><small>{event.detail}</small></span><em>{event.progress}%</em></div>):<small>Connecting to analysis stream…</small>}</div></section>}
